export type Form = {
  name: string;
  email: string;
  company: string;
  title: string;
  message: string;
};

export type ValidForm = {
  name: string;
  email: string;
  company: string;
  title: string;
  message: string;
};
